
 <%- include('../partials/header')  %> 

 <%- include ('../partials/main_nav') %>

  <!-- Main Content -->
  <main class="container">
    <div class="row">
        <div class="offset-md-1 col-md-11">
            <h1><%= title %></h1>
            <a href="/course/add" class="btn btn-primary" role="button">
            <i class="fas fa-plus-circle"></i> Add a course</a>
            <br>
            <table class="table table-bordered table-striped table-hover">
                <!-- Header Row-->
                <tr class="d-flex">
                    <th class="text-center col-3">Course Name</th>
                    <th class="text-center col-2">Course Detail</th>
                    <th class="text-center col-1"></th>
                    <th class="text-center col-1"></th>
                </tr>

                <!-- Repeatable Template Row -->

                <% for(let count = 0; count < CourseList.length; count++) { %>
                    <tr class="d-flex">
                        <td class="text-center col-3"><%= CourseList[count].name %></td>
                        <td class="text-center col-2"><%= CourseList[count].detail %></td>
                        <td class="text-center col-1">
                            <a href="/course/edit/<%= CourseList[count]._id %>" class="btn btn-primary btn-sm">
                            <i class="fas fa-pencil-alt"></i> Edit</a>
                        </td>
                        <td class="text-center col-1">
                            <a href="/course/delete/<%= CourseList[count]._id %>" class="btn btn-danger btn-sm">
                            <i class="fas fa-trash-alt"></i> Delete</a>
                        </td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
  </main>

  <%- include ('../partials/bottom_nav') %>  
  <%- include ('../partials/footer') %>
  

  





